# Install the NVIDIA PyTorch/torchvision builds matching each board's JetPack
# release before installing these architecture-independent packages.
loguru==0.7.2
numpy==1.24.4
paramiko==3.4.0
tqdm==4.66.4
ujson==5.10.0
